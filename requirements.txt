# Core dependencies
opencv-python==4.5.5.64  # OpenCV for image and video processing
pandas==1.3.5            # Pandas for data manipulation
ultralytics==8.0.20      # YOLO model library
cvzone==1.5.6            # CVZone for drawing utilities

# Labeling tool
labelImg==1.8.6          # LabelImg for labeling images

# Additional dependencies
numpy==1.21.6            # NumPy for numerical operations
matplotlib==3.5.1        # Matplotlib for visualization (if needed)

# Instructions for LabelImg
# After installing, run the following command in the terminal to start LabelImg:
# labelImg